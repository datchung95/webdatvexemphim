import { DISPLAY_LOADING, HIDDEN_LOADING } from "../types/LoadingType";

export const displayLoaidng = {
    type : DISPLAY_LOADING
}

export const hiddenLoading = {
    type : HIDDEN_LOADING
}